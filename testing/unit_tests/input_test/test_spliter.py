import os
import numpy as np
import unittest

from input import coil_sampler


class testSpliter(unittest.TestCase):

    def test_init(self):


        pass
        #dataset = CoILDataset(self.root_test_dir, transform=transforms.Compose([transforms.ToTensor()]))
        #data_loader = torch.utils.data.DataLoader(dataset, sampler, batch_size=120,
        #                                          shuffle=True, num_workers=12, pin_memory=True)




